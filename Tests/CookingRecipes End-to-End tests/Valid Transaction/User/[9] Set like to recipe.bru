meta {
  name: [9] Set like to recipe
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/likes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {{recipeId}}
}

assert {
  res.status: eq 200
}
