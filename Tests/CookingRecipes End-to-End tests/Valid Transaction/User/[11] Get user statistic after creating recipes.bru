meta {
  name: [11] Get user statistic after creating recipes
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/users/statistic
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status code 200", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
  
  test("recipesCount value is 1", function() {
    const data = res.getBody();
    expect(data.recipesCount).to.be.a("number");
    expect(data.recipesCount).to.equal(1);
  });
  
  test("likesCount value is 1", function() {
    const data = res.getBody();
    expect(data.likesCount).to.be.a("number");
    expect(data.likesCount).to.equal(1);
  });
  
  test("favouritesCount value is 1", function() {
    const data = res.getBody();
    expect(data.favouritesCount).to.be.a("number");
    expect(data.favouritesCount).to.equal(1);
  });
}
