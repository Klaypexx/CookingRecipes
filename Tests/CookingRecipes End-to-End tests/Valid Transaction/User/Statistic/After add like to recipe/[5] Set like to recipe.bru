meta {
  name: [5] Set like to recipe
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/likes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {{recipeId}}
}

assert {
  res.status: eq 200
}
