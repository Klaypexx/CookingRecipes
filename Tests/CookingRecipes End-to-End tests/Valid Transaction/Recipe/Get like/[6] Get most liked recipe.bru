meta {
  name: [6] Get most liked recipe
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/recipes/liked
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status code 200", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
