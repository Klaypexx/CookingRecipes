meta {
  name: [2] Check is authentificatied
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/isAuth
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}
