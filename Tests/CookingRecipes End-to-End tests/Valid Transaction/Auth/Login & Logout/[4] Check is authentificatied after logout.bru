meta {
  name: [4] Check is authentificatied after logout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/auth/isAuth
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 401
}
