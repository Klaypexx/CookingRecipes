meta {
  name: [4] Check is authentificated after refresh
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/auth/isAuth
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}
